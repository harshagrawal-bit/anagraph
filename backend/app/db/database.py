"""
Database setup — placeholder for Phase 1.
Phase 0 is stateless (no DB required).
Phase 1 will add PostgreSQL via SQLAlchemy for report storage and fund tenancy.
"""

# TODO Phase 1: Add SQLAlchemy engine, session factory, and Base here.
# from sqlalchemy import create_engine
# from sqlalchemy.orm import sessionmaker, DeclarativeBase
# from app.core.config import settings
#
# engine = create_engine(settings.DATABASE_URL)
# SessionLocal = sessionmaker(autocommit=False, autoflush=False, bind=engine)
#
# class Base(DeclarativeBase):
#     pass
